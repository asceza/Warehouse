## “ест AuthController в Postman

јвторизаци€
```json
https://localhost:44364/api/auth
body - raw - json

{
    "LastName" : "nameddd"
}
```


ƒобавление продукта
```json
https://localhost:44364/api/product
body - raw - json

{
    "Name" : "ѕродукт тестовый 1",
    "Article" : "111111",
    "Category" : 2,
    "Amount" : 111,
    "UnitOfMeasurement" : 4,
    "StoragePlace" : "111-111-111"
}
```
---
* ак в одном WebAPI контроллере asp core mvc содержать несколько методов*

¬ одном контроллере Web API в ASP.NET Core вы можете определ€ть несколько методов с различной логикой, использу€ атрибуты маршрутизации,
такие как `[HttpPost]`, `[HttpGet]`, `[HttpPut]`, `[HttpDelete]` и т.д. Ёто позвол€ет вам обрабатывать различные HTTP-методы
в одном контроллере. ќбратите внимание, что каждый метод должен иметь уникальный маршрут или использовать те же маршруты, но различатьс€ по методу HTTP.
- AddNewProduct: ќбрабатывает HTTP POST запросы дл€ добавлени€ нового продукта. ≈сли добавление не удалось, возвращает `BadRequest`, иначе возвращает `Ok` с результатом.
- GetAllProducts: ќбрабатывает HTTP GET запрос дл€ получени€ списка всех продуктов. ¬озвращает список продуктов с кодом ответа `Ok`.
- DeleteProduct: ќбрабатывает HTTP DELETE запросы дл€ удалени€ продукта по ID. ≈сли продукт не найден, возвращает `NotFound`, иначе возвращает `Ok` с результатом удалени€.
- GetProductById: ќбрабатывает HTTP GET запросы дл€ получени€ конкретного продукта по его ID. ≈сли продукт не найден, возвращает `NotFound`, иначе возвращает сам продукт с кодом ответа `Ok`.

ќписание маршрутов:
- ћетод `AddNewProduct` будет доступен по адресу `/api/product` и будет обрабатывать POST запросы.
- ћетод `GetAllProducts` будет доступен по адресу `/api/product` и будет обрабатывать GET запросы.
- ћетод `DeleteProduct` будет доступен по адресу `/api/product/{id}` и будет обрабатывать DELETE запросы, где `{id}` Ч это идентификатор продукта.
- ћетод `GetProductById` будет доступен по адресу `/api/product/{id}` и будет обрабатывать GET запросы.